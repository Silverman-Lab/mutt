% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{rename_and_align}
\alias{rename_and_align}
\title{Rename and align count matrices based on metadata and scale}
\usage{
rename_and_align(
  counts_reprocessed = NULL,
  counts_original = NULL,
  proportions_original = NULL,
  metadata,
  scale,
  by_col,
  align = TRUE,
  study_name = NULL
)
}
\arguments{
\item{counts_reprocessed}{A matrix or dataframe with rownames = Accessions (optional)}

\item{counts_original}{A matrix or dataframe with rownames = sample identifiers (optional)}

\item{proportions_original}{A matrix or dataframe with rownames = sample identifiers (optional)}

\item{metadata}{A dataframe with columns \code{Accession} and the desired renaming column(s)}

\item{scale}{A dataframe containing the list of valid samples}

\item{by_col}{A character vector specifying the column(s) in both \code{metadata} and \code{scale} to align by}

\item{align}{Logical; if FALSE, no renaming or alignment is performed}

\item{study_name}{Optional name of the study for logging purposes}
}
\value{
A list with three elements: \verb{$reprocessed}, \verb{$original}, and \verb{$proportions_original}
}
\description{
This function renames the rownames of \code{counts_reprocessed} using the \code{by_col}
column(s) in \code{metadata}, and aligns both \code{counts_reprocessed} and \code{counts_original}
to the samples listed in \code{scale[[by_col]]} if \code{align = TRUE}.
}
