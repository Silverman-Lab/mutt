% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{convert_repo_to_pkl}
\alias{convert_repo_to_pkl}
\title{Convert a microbial scale repository to a Python pickle file}
\usage{
convert_repo_to_pkl(microbialscalerepository, filepath)
}
\arguments{
\item{microbialscalerepository}{A named list of study objects, each of which
may contain a \code{tax} data.frame.}

\item{filepath}{Character. Path (including filename) where the \code{.pkl} file
should be written.}
}
\value{
Invisibly returns the value of \code{filepath} after writing.
}
\description{
Cleans a nested list of study objects by stripping out any \code{Date} elements,
applies \code{add_sequence_column()} to each study's \code{tax} slot (if present),
converts the result to a Python object via \strong{reticulate}, and saves it
to the specified \code{.pkl} file path.
}
\examples{
\dontrun{
convert_repo_to_pkl(microbialscalerepository, "output/microbiome.pkl")
}
}
