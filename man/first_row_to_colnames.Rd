% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{first_row_to_colnames}
\alias{first_row_to_colnames}
\title{Convert first row to column names}
\usage{
first_row_to_colnames(df)
}
\arguments{
\item{df}{A data frame whose first row contains the desired column names}
}
\value{
A data frame with the first row converted to column names and removed
from the data
}
\description{
Takes a data frame and uses its first row as column names, then removes
that row from the data. This is useful when reading data where the header
is in the first row of the data rather than as proper column names.
}
\examples{
\dontrun{
# Convert first row to column names
df <- data.frame(
  c("Name", "John", "Jane"),
  c("Age", "30", "25")
)
df <- first_row_to_colnames(df)
# Result: data frame with columns "Name" and "Age"
}

}
